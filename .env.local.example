# Local environment variables for development
# Copy this file to `.env.local` and fill the values. `.env.local` is ignored by Git.

# Gotenberg (Self-hosted Docker container, conversioni PDF illimitate)
# Avvia il container con: docker-compose up -d
# Per installare Docker Desktop: https://www.docker.com/products/docker-desktop
GOTENBERG_URL=http://localhost:3001
