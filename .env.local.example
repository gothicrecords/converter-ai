# Local environment variables for development
# Copy this file to `.env.local` and fill the values. `.env.local` is ignored by Git.

# Nessuna variabile necessaria per i converter offline.
# Opzionale: imposta `PORT` se vuoi cambiare la porta di sviluppo (default 3000)
# PORT=3000
